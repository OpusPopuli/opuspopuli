# Terraform state files (bootstrap uses local state)
*.tfstate
*.tfstate.*

# Terraform directory
.terraform/

# Crash logs
crash.log
crash.*.log

# Variable files with sensitive data
*.tfvars
!*.tfvars.example
