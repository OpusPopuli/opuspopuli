# Symlinks to shared assets (Prettier cannot process symlinks)
public/logos
public/icons
public/favicons
public/social

# Generated by scripts/refresh-schema-snapshot.mjs — reformatting it produces a
# large, meaningless diff on every regeneration. The freshness check compares
# schema meaning rather than bytes, so formatting no longer breaks CI, but
# leaving the file exactly as generated keeps its diffs readable.
__tests__/graphql/__fixtures__/schema.introspection.json
